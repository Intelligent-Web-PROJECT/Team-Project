
<%- include("../components/header") %>
<%- include("../components/navbar") %>

<h1>All Plants</h1>
<div class="row">
    <% plants.forEach(function(plant) { %>
        <div class="col-md-4 mb-4">
            <div class="card">

                <div class="card-body">
                    <% if (plant.photos.length > 0) { %>
                        <img src="data:<%= plant.photos[0]?.img_type %>;base64,<%= Buffer.from(plant.photos[0]?.img_data ?? "").toString('base64') %>" class="card-img-top" alt="<%= plant.name %>">
                    <% } %>
                    <h5 class="card-title"><%= plant.name %></h5>
                    <p class="card-text"><%= plant.description %></p>
                    <a href="/plants/<%= plant._id %>" class="btn btn-primary">View</a>
                </div>
            </div>
        </div>
    <% }); %>
</div>
<script src="/javascripts/indexDb.js"></script>
